spring.application.name=saloon_app

#spring.datasource.url=jdbc:postgresql://localhost:5432/postgres
spring.datasource.url=jdbc:postgresql://ep-steep-thunder-ahkv52r1-pooler.c-3.us-east-1.aws.neon.tech:5432/neondb?sslmode=require
spring.datasource.username=neondb_owner
spring.datasource.password=npg_WcgQB0VoZYi4

spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
#spring.jpa.properties.hibernate.format_sql=true

server.servlet.context-path=/api

server.port=${PORT:8080}

spring.servlet.multipart.max-file-size=20MB
spring.servlet.multipart.max-request-size=30MB


#logging.level.org.hibernate.SQL=DEBUG
#logging.level.org.hibernate.tool.hbm2ddl=DEBUG
#logging.level.org.springframework.orm.jpa=DEBUG

# --- Auth / JWT ---
# NOTE: dev-only placeholder. Override JWT_SECRET via env var in any real deployment.
jwt.secret=${JWT_SECRET:CHANGE_ME_dev_only_32_bytes_minimum_secret_key_value}
jwt.expiration-ms=604800000

# --- Firebase Cloud Messaging ---
# Path to a Firebase service-account JSON key file. Leave blank to disable push notifications (app still boots).
firebase.credentials.path=${FIREBASE_CREDENTIALS_PATH:}

# --- Smart Queue tuning ---
smart-queue.buffer-people-count=2
smart-queue.approaching-warning-minutes=15
smart-queue.sweep-fixed-delay-ms=45000

